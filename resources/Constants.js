/**
 * Constants
 */
export default class Constants {
  static DEVICES = 'DEVICES';
  static MODE = 'MODE';
  static GATEWAY_INTERVAL = 'GATEWAY_INTERVAL';
  static IP_SUFFIX = 'IP_SUFFIX';
  static NETWORK_PORT = 'NETWORK_PORT';
  static EXPORT_DIRECTORY = 'EXPORT_DIRECTORY';
}
